import { Address } from './address';
import { Credential } from './credential';

export class MarketPlaceUser {
    firstName: string;
    lastName: string;
    address: string;
    balance: number;
    email: string;
    id: number;
    password: string;
    phoneNumber: string;
    username: string;
    logged: boolean;
}
